plugins {
    id("io.github.magic-xu.magic-android-application")
    id("io.github.magic-xu.magic-android-compose")
}

android {
    namespace = "com.magic.platform.smoke.composeonly"

    defaultConfig {
        applicationId = "com.magic.platform.smoke.composeonly"
        versionCode = 1
        versionName = "1.0"
    }
}
